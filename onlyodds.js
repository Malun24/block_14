const OddArray = (odd) => {
    const newArray = [];

    for (let i = 1; i <= 100; i++) {
        if (i % 1 === 0 && i % 2 === 0) {
        }
    }

    return newArray;
};

console.log(newArray([2, 4, 6, 8, 11, 20, 15, 22]));